FROM nginx:1.27-alpine

# Prebuilt static assets copied from local build output
COPY apps/web/dist /usr/share/nginx/html
COPY apps/web/nginx.spa.conf /etc/nginx/conf.d/default.conf

EXPOSE 80

CMD ["nginx", "-g", "daemon off;"]